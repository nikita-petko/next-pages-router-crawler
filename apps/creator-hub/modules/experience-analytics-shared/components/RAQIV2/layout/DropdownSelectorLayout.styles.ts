import { makeStyles } from '@rbx/ui';

const useDropdownSelectorLayoutStyles = makeStyles()(() => ({
  selectorWidth: {
    minWidth: '220px',
  },
}));

export default useDropdownSelectorLayoutStyles;
