import { makeStyles } from '@rbx/ui';

const useTransactionsContainerStyles = makeStyles()(() => ({
  tabWithIcon: {
    marginBottom: '-6px',
  },

  tabContainer: {
    width: '100%',
  },
}));
export default useTransactionsContainerStyles;
