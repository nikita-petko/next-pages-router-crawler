import { makeStyles } from '@rbx/ui';

const useAssociatedItemsGridContainerStyles = makeStyles()(() => ({
  actionButton: {
    marginTop: 32,
    marginBottom: 32,
  },
}));

export default useAssociatedItemsGridContainerStyles;
