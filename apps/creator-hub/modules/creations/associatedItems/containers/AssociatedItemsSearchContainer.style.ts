import { makeStyles } from '@rbx/ui';

const useAssociatedItemsSearchContainerStyles = makeStyles()(() => ({
  searchAssetTextField: {
    marginBottom: 16,
    marginTop: 32,
  },

  clearSearchIcon: {
    cursor: 'pointer',
    opacity: '68%',
  },
}));

export default useAssociatedItemsSearchContainerStyles;
