import { makeStyles } from '@rbx/ui';

const useEventAnalyticsContainerStyles = makeStyles()(() => ({
  analyticsContainer: {
    padding: '0px 24px',
    marginTop: 8,
  },
}));

export default useEventAnalyticsContainerStyles;
