import { makeStyles } from '@rbx/ui';

const useNotificationContentListStyles = makeStyles()({
  createNotificationContentButton: {
    margin: '36px 0',
  },
  tableHeadTitle: {
    whiteSpace: 'nowrap',
  },
  paginationStyle: {
    borderBottom: 0,
  },
  actionsProgress: {
    margin: 12,
  },
});

export default useNotificationContentListStyles;
