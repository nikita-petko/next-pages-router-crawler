import { makeStyles } from '@rbx/ui';

const useOverviewIsActiveLabelStyles = makeStyles()((theme) => ({
  overviewLabel: {
    marginTop: theme.spacing(0.5),
  },
}));

export default useOverviewIsActiveLabelStyles;
