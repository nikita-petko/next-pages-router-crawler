import { makeStyles } from '@rbx/ui';

const useItemCardActivityStyles = makeStyles()((theme) => ({
  typography: {
    color: theme.palette.success.main,
  },
}));

export default useItemCardActivityStyles;
