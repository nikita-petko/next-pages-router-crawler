import { makeStyles } from '@rbx/ui';

const useItemGridContainerStyles = makeStyles()(() => ({
  itemGridFailureViewContainer: {
    width: `100%`,
    height: 'auto',
    minHeight: 400,
  },
}));

export default useItemGridContainerStyles;
