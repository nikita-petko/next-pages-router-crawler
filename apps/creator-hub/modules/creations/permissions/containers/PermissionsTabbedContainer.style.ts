import { makeStyles } from '@rbx/ui';

const UsePermissionsTabbedContainerStyles = makeStyles()(() => ({
  sectionHeader: {
    paddingBottom: 16,
  },
  permissionTabs: {
    marginBottom: 8,
  },
}));

export default UsePermissionsTabbedContainerStyles;
