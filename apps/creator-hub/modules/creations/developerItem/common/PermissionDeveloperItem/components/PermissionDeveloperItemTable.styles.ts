import { makeStyles } from '@rbx/ui';

const usePermissionDeveloperItemTableStyles = makeStyles()({
  avatarCell: {
    columnGap: 15,
  },
  buttonText: {
    textTransform: 'none',
  },
  paginationStyle: {
    borderBottom: 0,
  },
});

export default usePermissionDeveloperItemTableStyles;
