import { makeStyles } from '@rbx/ui';

const usePermissionDeveloperItemExperiencesTabStyles = makeStyles()(() => ({
  container: {
    padding: 8,
  },
}));

export default usePermissionDeveloperItemExperiencesTabStyles;
