import { makeStyles } from '@rbx/ui';

const usePermissionDeveloperItemWithTabsStyles = makeStyles()(() => ({
  alert: {
    borderRadius: 8,
    marginBottom: 8,
  },
  container: {
    padding: 8,
  },
  headerContainer: {
    marginBottom: 16,
  },
  iconButton: {
    padding: 0,
  },
  tabPanel: {
    marginTop: 16,
  },
}));

export default usePermissionDeveloperItemWithTabsStyles;
