import { makeStyles } from '@rbx/ui';

const useUploadImagePreviewsFormStyles = makeStyles()((theme) => ({
  headerTitle: {
    marginBottom: theme.spacing(1),
  },
  headerDescription: {
    marginBottom: theme.spacing(1),
  },
}));

export default useUploadImagePreviewsFormStyles;
