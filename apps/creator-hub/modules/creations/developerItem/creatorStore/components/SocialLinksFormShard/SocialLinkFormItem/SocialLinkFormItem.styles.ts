import { makeStyles } from '@rbx/ui';

const useSocialLinkFormItemStyles = makeStyles()(() => ({
  formContainer: {
    marginBottom: 8,
  },

  urlField: {
    marginTop: 2,
  },
}));

export default useSocialLinkFormItemStyles;
