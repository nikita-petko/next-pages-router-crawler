import { makeStyles } from '@rbx/ui';

const usePricingAlertStyles = makeStyles()(() => ({
  alert: {
    marginBottom: '18px',
  },
  alertTitle: {
    lineHeight: '22px',
  },
}));

export default usePricingAlertStyles;
