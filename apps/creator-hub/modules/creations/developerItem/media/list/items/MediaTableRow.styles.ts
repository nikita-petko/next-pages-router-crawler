import { makeStyles } from '@rbx/ui';

const useMediaTableRowStyles = makeStyles()(() => ({
  fixWidthColumn: {
    width: 172,
  },
  tableLink: {
    display: 'contents',
  },
}));

export default useMediaTableRowStyles;
