import { makeStyles } from '@rbx/ui';

const useMarkdownParserStyles = makeStyles()({
  headerFour: {
    marginBottom: `16px`,
  },
  unorderedList: {
    margin: `0px`,
  },
  orderedList: {
    margin: `0px`,
    paddingLeft: `24px`,
  },
});

export default useMarkdownParserStyles;
