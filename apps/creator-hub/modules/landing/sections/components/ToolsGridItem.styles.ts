import { makeStyles } from '@rbx/ui';

const useToolsGridItemStyles = makeStyles()(() => ({
  root: {
    padding: 16,
  },

  heading: {
    paddingBottom: 8,
  },

  content: {
    paddingLeft: 8,
  },
}));

export default useToolsGridItemStyles;
