import { makeStyles } from '@rbx/ui';

const useExperienceQuestionnaireStyles = makeStyles()(() => ({
  root: {
    width: '100%',
    margin: 'auto',
  },
}));

export default useExperienceQuestionnaireStyles;
