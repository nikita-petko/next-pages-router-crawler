import { makeStyles } from '@rbx/ui';

const usePublishAppDialogCardStyles = makeStyles()(() => ({
  alertLabel: {
    marginBottom: 24,
  },

  textBlock: {
    marginTop: 16,
    padding: 16,
  },

  disclaimerBlock: {
    padding: 16,
  },

  disclaimerText: {
    marginLeft: 20,
  },
}));

export default usePublishAppDialogCardStyles;
