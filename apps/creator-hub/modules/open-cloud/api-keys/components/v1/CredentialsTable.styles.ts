import { makeStyles } from '@rbx/ui';

const useCredentialsTableStyles = makeStyles()(() => ({
  createButton: {
    marginTop: 16,
    marginBottom: 16,
  },

  emptyGrid: {
    marginTop: 24,
  },
}));

export default useCredentialsTableStyles;
