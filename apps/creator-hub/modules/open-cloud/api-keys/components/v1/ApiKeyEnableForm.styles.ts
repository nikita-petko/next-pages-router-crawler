import { makeStyles } from '@rbx/ui';

const useApiEnableFormStyles = makeStyles()(() => ({
  status: {
    marginLeft: 8,
  },

  label: {
    marginTop: 32,
  },
}));

export default useApiEnableFormStyles;
