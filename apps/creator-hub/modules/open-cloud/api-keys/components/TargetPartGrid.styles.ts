import { makeStyles } from '@rbx/ui';

const useTargetPartGridStyles = makeStyles()(() => ({
  inputContainer: {
    marginTop: 16,
  },

  startAdornment: {
    marginRight: 8,
  },
}));

export default useTargetPartGridStyles;
