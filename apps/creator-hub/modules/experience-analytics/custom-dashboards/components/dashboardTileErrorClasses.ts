/** Shared Foundation Tailwind classes for per-tile empty/error states. */

/** Centered empty/error shell matching Figma 2384-62450 (icon + title + secondary). */
export const DASHBOARD_TILE_EMPTY_STATE_CLASSES =
  'flex flex-col items-center justify-center gap-xsmall min-height-[120px] width-full min-width-0 padding-medium text-align-x-center';
