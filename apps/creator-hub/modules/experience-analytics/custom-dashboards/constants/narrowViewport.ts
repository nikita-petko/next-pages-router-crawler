/**
 * Matches view/preview chart collapse and chart-editor stacking. Keep this synchronized with
 * the 900px CSS-module media query and `max-[900px]` Foundation Tailwind variants.
 */
export const CUSTOM_DASHBOARD_NARROW_VIEWPORT_MAX_WIDTH_PX = 900;

export const CUSTOM_DASHBOARD_NARROW_VIEWPORT_MEDIA_QUERY = `(max-width: ${CUSTOM_DASHBOARD_NARROW_VIEWPORT_MAX_WIDTH_PX}px)`;
