import { makeStyles } from '@rbx/ui';

const useTabContentStyles = makeStyles()(() => ({
  container: {
    margin: 0,
  },

  gridItem: {
    paddingLeft: 0,
  },

  buttonContainer: {
    paddingLeft: 0,
    marginTop: 24,
  },
}));

export default useTabContentStyles;
