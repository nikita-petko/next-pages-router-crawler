import { makeStyles } from '@rbx/ui';

const UseBanHistoryDialogContentStyles = makeStyles()(() => ({
  dialogContentContainer: {
    paddingLeft: 35,
  },
  endIndicator: {
    padding: '0 0 20px 40px',
  },
}));

export default UseBanHistoryDialogContentStyles;
