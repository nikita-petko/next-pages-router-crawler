import { makeStyles } from '@rbx/ui';

const useUniverseOverviewThumbnailStyles = makeStyles()(() => ({
  container: {
    float: 'left',
    borderRadius: 12,
  },
}));

export default useUniverseOverviewThumbnailStyles;
