import { makeStyles } from '@rbx/ui';

const useImageEntrySorterAndSearcherStyles = makeStyles()((theme) => ({
  statusIcon: {
    display: 'inline-flex',
    alignItems: 'center',
    marginLeft: theme.spacing(1),
  },
}));

export default useImageEntrySorterAndSearcherStyles;
