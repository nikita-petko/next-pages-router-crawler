import { makeStyles } from '@rbx/ui';

const useTextFilterWarningStyles = makeStyles()((theme) => ({
  divider: {
    marginTop: 30,
  },

  heading: {
    marginTop: 10,
    marginBottom: 5,
  },

  text: {
    maginTop: 5,
    marginBottom: 5,
  },
}));

export default useTextFilterWarningStyles;
