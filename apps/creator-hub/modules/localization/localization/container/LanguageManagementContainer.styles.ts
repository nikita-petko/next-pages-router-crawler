import { makeStyles } from '@rbx/ui';

const useLanguageManagementContainerStyles = makeStyles()((theme) => ({
  errorText: {
    marginTop: theme.spacing(4),
  },
}));

export default useLanguageManagementContainerStyles;
