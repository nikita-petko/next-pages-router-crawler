import { makeStyles } from '@rbx/ui';

const useIconImageUploaderStyles = makeStyles()((theme) => ({
  iconHeader: {
    marginTop: 10,
    marginBottom: 10,
  },

  text: {
    maginTop: 5,
    marginBottom: 5,
  },

  reviewText: {
    maginTop: 5,
    marginBottom: 30,
  },

  button: {
    paddingTop: 30,
  },
}));

export default useIconImageUploaderStyles;
