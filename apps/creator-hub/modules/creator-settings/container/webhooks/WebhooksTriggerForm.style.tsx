import { makeStyles } from '@rbx/ui';

const useWebhooksTriggerFormStyles = makeStyles()(() => ({
  accordion: {
    marginTop: 12,
  },
  container: {
    rowGap: 12,
  },
}));

export default useWebhooksTriggerFormStyles;
