export enum ExperimentDetailsTab {
  DetailsAndProgress = 'details_and_progress',
  Results = 'results',
}

export enum ExperimentUIStatus {
  DecisionNeeded = 'DecisionNeeded',
  Running = 'Running',
  Scheduled = 'Scheduled',
  Completed = 'Completed',
  Draft = 'Draft',
}
