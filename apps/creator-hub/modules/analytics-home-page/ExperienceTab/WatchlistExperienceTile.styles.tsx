import { makeStyles } from '@rbx/ui';

const useWatchlistExperienceTileStyles = makeStyles()(() => ({
  closeButton: {
    position: 'absolute',
    top: '-12px',
    right: '-12px',
  },
}));

export default useWatchlistExperienceTileStyles;
