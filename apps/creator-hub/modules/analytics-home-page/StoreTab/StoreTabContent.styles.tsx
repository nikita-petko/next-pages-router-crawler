import { makeStyles } from '@rbx/ui';

const useStoreTabContentStyles = makeStyles()(() => ({
  paymentTypeDropdown: {
    minWidth: '200px',
  },

  opportunityInsights: {
    marginTop: '16px',
  },
}));

export default useStoreTabContentStyles;
