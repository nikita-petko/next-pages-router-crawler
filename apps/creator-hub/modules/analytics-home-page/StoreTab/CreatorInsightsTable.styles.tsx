import { makeStyles } from '@rbx/ui';

const useCreatorInsightsTableStyles = makeStyles()(() => ({
  noResultsContainer: {
    padding: '70px 0px',
  },
}));

export default useCreatorInsightsTableStyles;
