import { makeStyles } from '@rbx/ui';

const useTableExportButtonStyles = makeStyles()(() => ({
  button: {
    padding: '8px 10px',
    minWidth: 0,
  },
}));

export default useTableExportButtonStyles;
