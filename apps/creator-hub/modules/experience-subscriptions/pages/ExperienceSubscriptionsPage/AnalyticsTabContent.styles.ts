import { makeStyles } from '@rbx/ui';

const useAnalyticsTabContentStyles = makeStyles()((theme) => ({
  description: {
    marginTop: '12px',
    marginBottom: '48px',
  },
}));

export default useAnalyticsTabContentStyles;
