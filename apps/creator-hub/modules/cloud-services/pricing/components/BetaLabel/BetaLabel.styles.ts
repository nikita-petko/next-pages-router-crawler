import { makeStyles } from '@rbx/ui';

const useBetaLabelStyles = makeStyles()(() => {
  return {
    label: {
      marginLeft: 8,
      alignSelf: 'center',
    },
  };
});

export default useBetaLabelStyles;
