import { makeStyles } from '@rbx/ui';

const useBillingStatementLeftNavigationStyles = makeStyles()(() => ({
  navigateIcon: {
    paddingTop: 2,
  },
  backSection: {
    display: 'flex',
  },
}));

export default useBillingStatementLeftNavigationStyles;
