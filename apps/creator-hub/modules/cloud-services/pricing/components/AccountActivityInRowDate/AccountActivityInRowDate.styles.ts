import { makeStyles } from '@rbx/ui';

const useAccountActivityInRowDateStyles = makeStyles()(() => ({
  descriptionIcon: {
    paddingRight: 4,
    paddingBottom: 2,
    marginTop: 2,
  },
}));

export default useAccountActivityInRowDateStyles;
