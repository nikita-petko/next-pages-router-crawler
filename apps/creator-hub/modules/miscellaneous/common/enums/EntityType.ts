/**
 * Type of entity the image will be uploaded for.
 * Asset - image is uploaded for the asset.
 * Experience -  an image uploaded for the experience.
 */
enum EntityType {
  Asset = 'asset',
  Experience = 'experience',
}

export default EntityType;
