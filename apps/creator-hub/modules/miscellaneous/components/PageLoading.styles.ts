import { makeStyles } from '@rbx/ui';

const usePageLoadingStyles = makeStyles()({
  root: {
    flexGrow: 1,
    width: '100%',
    height: '100%',
    minHeight: 200,
  },
});

export default usePageLoadingStyles;
