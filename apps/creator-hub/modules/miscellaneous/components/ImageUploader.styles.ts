import { makeStyles } from '@rbx/ui';

const useImageUploaderStyles = makeStyles()(() => ({
  information: {
    padding: '4px 12px',
  },

  inputStyle: {
    display: 'none',
  },
}));

export default useImageUploaderStyles;
