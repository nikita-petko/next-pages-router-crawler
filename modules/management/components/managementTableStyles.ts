import { makeStyles } from '@rbx/ui';

const useManagementTableStyles = makeStyles()(() => ({
  robuxIconContainer: {
    alignItems: 'center',
    columnGap: '4px',
    display: 'flex',
    justifyContent: 'flex-end',
  },
}));

export default useManagementTableStyles;
