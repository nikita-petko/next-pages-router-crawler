import { makeStyles } from '@rbx/ui';

const useManageAdsEducationStyles = makeStyles()(() => ({
  outerContainer: {
    alignItems: 'flex-start',
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
  },
  pageTitle: {
    marginBottom: '48px',
    marginTop: '48px',
  },
}));

export default useManageAdsEducationStyles;
