import { makeStyles } from '@rbx/ui';

const useSpendObjectivePlatformToggleStyles = makeStyles()(() => ({
  container: {
    alignItems: 'center',
    display: 'flex',
    gap: '8px',
    marginTop: '8px',
  },
}));

export default useSpendObjectivePlatformToggleStyles;
