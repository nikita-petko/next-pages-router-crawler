import { makeStyles } from '@rbx/ui';

const useDateFilteringNoDataPageStyles = makeStyles()(() => ({
  centered: {
    alignItems: 'center',
    display: 'flex',
    justifyContent: 'center',
    position: 'relative',
    top: 282,
    width: '100%',
  },
}));

export default useDateFilteringNoDataPageStyles;
