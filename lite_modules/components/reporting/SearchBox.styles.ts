import { makeStyles } from '@rbx/ui';

const useSearchBoxStyles = makeStyles()(() => ({
  closeButton: {
    marginRight: '8px',
  },

  searchBox: {
    width: '300px',
  },
}));

export default useSearchBoxStyles;
