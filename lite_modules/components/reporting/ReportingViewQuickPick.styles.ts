import { makeStyles } from '@rbx/ui';

const useReportingViewQuickPickStyles = makeStyles()(() => ({
  reportingViewQuickPickContainer: {
    width: '200px',
  },
}));

export default useReportingViewQuickPickStyles;
