import { makeStyles } from '@rbx/ui';

const useExperienceFilterPickerStyles = makeStyles()(() => ({
  experiencePicker: {
    width: '300px',
  },
}));

export default useExperienceFilterPickerStyles;
