import { makeStyles } from '@rbx/ui';

const useInfoTooltipStyles = makeStyles()(() => ({
  tooltip: {
    height: 16,
    marginLeft: 4,
    width: 16,
  },
}));

export default useInfoTooltipStyles;
