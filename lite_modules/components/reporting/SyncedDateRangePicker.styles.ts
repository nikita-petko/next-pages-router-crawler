import { makeStyles } from '@rbx/ui';

const useSyncedDateRangePickerStyles = makeStyles()(() => ({
  syncedDateRangePickerContainer: {
    width: '280px',
  },
}));

export default useSyncedDateRangePickerStyles;
