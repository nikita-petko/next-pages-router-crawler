import { useContext } from 'react';
import { CueContext, type UseCueResult } from './cueContext';

export const useCue = (): UseCueResult => {
  const cue = useContext(CueContext);
  if (!cue) {
    throw new Error('useCue must be used within a CueProvider');
  }
  return cue;
};
