import {
  getAppealsPortalUrl,
  getRegistryEntry,
  getSafetyDashboardAppealsUrl,
  isOverrideBackedAbuseVector
} from "./chunk-GSIEHWVZ.js";

// src/treatments/dialog/UniversalFeatureRestrictionDialog.tsx
import { useState as useState2 } from "react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { Dialog, DialogContent } from "@rbx/foundation-ui";

// src/contexts/RestrictionScopeContext.tsx
import { createContext, useContext, useMemo } from "react";
import { jsx } from "react/jsx-runtime";
var RestrictionScopeContext = createContext(void 0);
var RestrictionScopeProvider = ({ abuseVector, readOnly = false, children }) => {
  const scope = useMemo(() => ({ abuseVector, readOnly }), [abuseVector, readOnly]);
  return /* @__PURE__ */ jsx(RestrictionScopeContext.Provider, { value: scope, children });
};
var useRestrictionScope = () => {
  const scope = useContext(RestrictionScopeContext);
  if (!scope) {
    throw new Error("useRestrictionScope must be used within a RestrictionScopeProvider");
  }
  return scope;
};

// src/contexts/UniversalFeatureRestrictionsConfigContext.tsx
import { createContext as createContext2, useContext as useContext2 } from "react";
import { jsx as jsx2 } from "react/jsx-runtime";
var UniversalFeatureRestrictionsConfigContext = createContext2(void 0);
var UniversalFeatureRestrictionsConfigProvider = ({ config, children }) => {
  return /* @__PURE__ */ jsx2(UniversalFeatureRestrictionsConfigContext.Provider, { value: config, children });
};
var useUniversalFeatureRestrictionsConfig = () => {
  const config = useContext2(UniversalFeatureRestrictionsConfigContext);
  if (!config) {
    throw new Error(
      "useUniversalFeatureRestrictionsConfig must be used within a UniversalFeatureRestrictionsConfigProvider"
    );
  }
  return config;
};

// src/treatments/dialog/DialogContentView.tsx
import { DialogBody, DialogTitle } from "@rbx/foundation-ui";

// src/treatments/dialog/useDialogRestrictionModel.ts
import { useEffect as useEffect2, useRef } from "react";

// src/api/useModerationDetail.ts
import { useQuery } from "@tanstack/react-query";
var moderationDetailQueryKey = (abuseVector) => [
  "moderation-detail",
  abuseVector
];
var useModerationDetail = () => {
  const { api } = useUniversalFeatureRestrictionsConfig();
  const { abuseVector } = useRestrictionScope();
  const { data, isLoading, isFetching, error } = useQuery({
    queryKey: moderationDetailQueryKey(abuseVector),
    queryFn: () => api.fetchModerationDetail(abuseVector),
    enabled: !isOverrideBackedAbuseVector(abuseVector)
  });
  return { data, isLoading, isFetching, error };
};

// src/hooks/useRestrictionEndDateInfo.ts
import { useState, useMemo as useMemo2, useEffect } from "react";

// src/shared/utils/formatCountdown.ts
var ONE_MINUTE_IN_SECONDS = 60;
var ONE_HOUR_IN_SECONDS = 3600;
var ONE_DAY_IN_SECONDS = 86400;
function formatCountdown(timeRemainingSecs, translate) {
  if (timeRemainingSecs <= 0) {
    return void 0;
  }
  const roundedUpMinutes = Math.ceil(timeRemainingSecs / ONE_MINUTE_IN_SECONDS);
  const totalSeconds = roundedUpMinutes * ONE_MINUTE_IN_SECONDS;
  if (totalSeconds >= ONE_DAY_IN_SECONDS) {
    const days = Math.ceil(totalSeconds / ONE_DAY_IN_SECONDS);
    if (days === 1) {
      return translate("Label.TimeLeft.Day.Singular");
    }
    return translate("Label.TimeLeft.Day.Plural", { number: String(days) });
  }
  const hours = Math.floor(totalSeconds / ONE_HOUR_IN_SECONDS);
  const minutes = Math.floor(totalSeconds % ONE_HOUR_IN_SECONDS / ONE_MINUTE_IN_SECONDS);
  const hh = String(hours).padStart(2, "0");
  const mm = String(minutes).padStart(2, "0");
  return translate("Label.TimeLeft.Hours", { time: `${hh}:${mm}` });
}

// src/hooks/useRestrictionEndDateInfo.ts
function getEndDateTimestamp(endDateString) {
  if (!endDateString) {
    return void 0;
  }
  const timestamp = new Date(endDateString).getTime();
  return Number.isNaN(timestamp) ? void 0 : timestamp;
}
function getRemainingSeconds(endTimestamp) {
  return Math.max((endTimestamp - Date.now()) / 1e3, 0);
}
var useRestrictionEndDateInfo = (endDateString) => {
  const { translate, locale } = useUniversalFeatureRestrictionsConfig();
  const endTimestamp = getEndDateTimestamp(endDateString);
  const [countdownText, setCountdownText] = useState(() => {
    if (!endTimestamp) {
      return void 0;
    }
    return formatCountdown(getRemainingSeconds(endTimestamp), translate);
  });
  const formattedEndDate = useMemo2(() => {
    if (!endTimestamp) {
      return void 0;
    }
    return new Intl.DateTimeFormat(locale, {
      year: "numeric",
      month: "long",
      day: "numeric"
    }).format(new Date(endTimestamp));
  }, [endTimestamp, locale]);
  useEffect(() => {
    if (!endTimestamp) {
      setCountdownText(void 0);
      return;
    }
    const tick = () => {
      const remaining = getRemainingSeconds(endTimestamp);
      setCountdownText(formatCountdown(remaining, translate));
      return remaining;
    };
    if (tick() <= 0) {
      return;
    }
    const intervalId = setInterval(() => {
      if (tick() <= 0) {
        clearInterval(intervalId);
      }
    }, 1e3);
    return () => {
      clearInterval(intervalId);
    };
  }, [endTimestamp, translate]);
  return { formattedEndDate, countdownText };
};

// src/telemetry/useSendAnalyticsEvent.ts
import { useCallback } from "react";

// src/telemetry/eventConstants.ts
var EVENT_NAME = "InExperienceInterventionEvent";
var EVENT_CONTEXT = "InExperienceInterventionEvent";
var INTERVENTION_UFR_NUDGE = "INTERVENTION_UFR_NUDGE";
var INTERVENTION_UFR_TIMEOUT = "INTERVENTION_UFR_TIMEOUT";

// src/telemetry/analyticsContracts.ts
function buildAnalyticsEvent(eventType, abuseVector, placement, additionalFields) {
  return {
    name: EVENT_NAME,
    context: EVENT_CONTEXT,
    properties: {
      timestamp_milliseconds: Date.now(),
      event_type: eventType,
      abuse_vector: abuseVector,
      placement,
      // Optional fields passed in from consumers parsed into snake_case fields
      event_id: additionalFields?.eventId,
      timeout_duration_seconds: additionalFields?.timeoutDurationSeconds,
      acknowledgeable: additionalFields?.acknowledgeable,
      intervention_type: additionalFields?.interventionType,
      evidence: additionalFields?.evidence,
      time_to_interact_seconds: additionalFields?.timeToInteractSeconds,
      error: additionalFields?.error
    }
  };
}

// src/telemetry/useSendAnalyticsEvent.ts
function useSendAnalyticsEvent() {
  const { sendAnalyticsEvent, placement } = useUniversalFeatureRestrictionsConfig();
  const { abuseVector, readOnly } = useRestrictionScope();
  return useCallback(
    (eventType, additionalFields) => {
      if (readOnly) {
        return;
      }
      sendAnalyticsEvent(buildAnalyticsEvent(eventType, abuseVector, placement, additionalFields));
    },
    [abuseVector, placement, readOnly, sendAnalyticsEvent]
  );
}

// src/shared/utils/parseBadUtterances.ts
var parseBadUtterances = (badUtterances, translate) => {
  const textItems = [];
  const abuseTypeSet = /* @__PURE__ */ new Set();
  badUtterances.forEach((utterance) => {
    textItems.push(utterance.utteranceText);
    abuseTypeSet.add(translate(utterance.labelTranslationKey));
  });
  return {
    textItems: textItems.reverse(),
    abuseTypes: [...abuseTypeSet].filter(Boolean)
  };
};

// src/shared/utils/time.ts
var NANOSECONDS_PER_SECOND = 1e9;
var MILLISECONDS_PER_SECOND = 1e3;
function monotonicNowMs() {
  return performance.now();
}

// src/treatments/dialog/restrictionDialogContent/resolveRestrictionDialogContent.ts
function resolveDurationUnit(durationNs) {
  const totalSeconds = durationNs / NANOSECONDS_PER_SECOND;
  const minutes = Math.ceil(totalSeconds / 60);
  if (minutes < 60) {
    return { unit: "Minute", number: minutes };
  }
  const hours = Math.ceil(minutes / 60);
  if (hours < 24) {
    return { unit: "Hour", number: hours };
  }
  const days = Math.ceil(hours / 24);
  return { unit: "Day", number: days };
}
var SUSPENDED_TITLE_KEYS = {
  Minute: {
    singular: "Generic.DialogTitle.Suspended.Minute",
    plural: "Generic.DialogTitle.Suspended.Minutes"
  },
  Hour: {
    singular: "Generic.DialogTitle.Suspended.Hour",
    plural: "Generic.DialogTitle.Suspended.Hours"
  },
  Day: {
    singular: "Generic.DialogTitle.Suspended.Day",
    plural: "Generic.DialogTitle.Suspended.Days"
  }
};
function resolveTitleDescriptor({
  interventionType,
  durationNs,
  abuseVector
}) {
  const registryTitleKey = getRegistryEntry(abuseVector)?.titleKeys?.[interventionType];
  if (registryTitleKey) {
    return { key: registryTitleKey };
  }
  if (interventionType === "Nudge") {
    return { key: "Generic.DialogTitle.Nudge" };
  }
  if (interventionType === "Banned") {
    return { key: "Generic.DialogTitle.Banned" };
  }
  const { unit, number } = resolveDurationUnit(durationNs);
  const keys = SUSPENDED_TITLE_KEYS[unit];
  return number === 1 ? { key: keys.singular } : { key: keys.plural, params: { number: String(number) } };
}
function resolveBodyDescriptor({
  interventionType,
  abuseVector
}) {
  const bodyKey = getRegistryEntry(abuseVector)?.bodyKeys?.[interventionType];
  if (bodyKey) {
    return { key: bodyKey };
  }
  return { key: `Generic.DialogBody.${interventionType}` };
}
function resolveAbuseVectorLabel(abuseVector, translate, overrideLabel) {
  const entry = getRegistryEntry(abuseVector);
  if (entry) {
    return translate(entry.labelKey);
  }
  if (overrideLabel) {
    return overrideLabel;
  }
  return abuseVector.split("_").map((word) => word.length > 0 ? `${word.charAt(0).toUpperCase()}${word.slice(1)}` : word).join(" ");
}

// src/shared/utils/determineInterventionType.ts
var determineInterventionType = ({
  punishmentTypeDescription,
  durationNs
}) => {
  let interventionType;
  if (punishmentTypeDescription?.toLowerCase().includes("nudge")) {
    interventionType = "Nudge";
  } else if (Number.isFinite(durationNs) && durationNs > 0) {
    interventionType = "Suspended";
  } else {
    interventionType = "Banned";
  }
  const interventionTypeForAnalytics = interventionType === "Nudge" ? INTERVENTION_UFR_NUDGE : INTERVENTION_UFR_TIMEOUT;
  return { interventionType, interventionTypeForAnalytics };
};

// src/shared/utils/determineAppealability.ts
var APPEALS_THRESHOLD_SECONDS = 30 * 60;
function isDurationAppealable(durationNs) {
  if (durationNs <= 0) {
    return false;
  }
  return durationNs / NANOSECONDS_PER_SECOND > APPEALS_THRESHOLD_SECONDS;
}
function determineAppealability({
  beginDate,
  endDate,
  acknowledgeable
}) {
  if (!beginDate || !endDate) {
    return acknowledgeable;
  }
  const beginTimestamp = new Date(beginDate).getTime();
  const endTimestamp = new Date(endDate).getTime();
  if (Number.isNaN(beginTimestamp) || Number.isNaN(endTimestamp)) {
    return acknowledgeable;
  }
  const durationSeconds = (endTimestamp - beginTimestamp) / 1e3;
  if (durationSeconds <= 0) {
    return acknowledgeable;
  }
  return durationSeconds > APPEALS_THRESHOLD_SECONDS;
}

// src/treatments/dialog/useDialogRestrictionModel.ts
var useDialogRestrictionModel = ({
  overrides,
  translationsReady
}) => {
  const mountTimeMsRef = useRef();
  const sendAnalyticsEvent = useSendAnalyticsEvent();
  const { translate } = useUniversalFeatureRestrictionsConfig();
  const { abuseVector } = useRestrictionScope();
  const { data: moderationDetail, isLoading, isFetching, error } = useModerationDetail();
  const overrideBacked = isOverrideBackedAbuseVector(abuseVector);
  const hasUnusableDetail = !overrideBacked && !moderationDetail?.interventionId;
  const endDateSource = overrideBacked ? overrides?.restriction?.endDate : moderationDetail?.endDate;
  const { formattedEndDate, countdownText } = useRestrictionEndDateInfo(
    translationsReady && !hasUnusableDetail ? endDateSource : void 0
  );
  useEffect2(() => {
    if (!translationsReady || isFetching) {
      return;
    }
    if (error) {
      sendAnalyticsEvent("GetModerationDetailFailed" /* GetModerationDetailFailed */, {
        error: error.message
      });
      return;
    }
    if (hasUnusableDetail) {
      sendAnalyticsEvent("GetModerationDetailFailed" /* GetModerationDetailFailed */, {
        error: "Moderation detail returned without usable info"
      });
    }
  }, [error, isFetching, hasUnusableDetail, sendAnalyticsEvent, translationsReady]);
  if (!translationsReady || !overrideBacked && isLoading) {
    return { status: "loading" };
  }
  if (error || hasUnusableDetail) {
    return { status: "error" };
  }
  const {
    acknowledgeable = false,
    badUtterances,
    beginDate,
    consequenceTransparencyMessage,
    duration,
    endDate,
    interventionId,
    punishmentTypeDescription
  } = moderationDetail ?? {};
  const resolvedDuration = duration ?? overrides?.restriction?.duration ?? 0;
  const isAppealable = moderationDetail ? determineAppealability({
    beginDate,
    endDate,
    acknowledgeable
  }) : isDurationAppealable(resolvedDuration);
  const { interventionType, interventionTypeForAnalytics } = determineInterventionType({
    punishmentTypeDescription,
    durationNs: resolvedDuration
  });
  const { textItems, abuseTypes } = moderationDetail ? parseBadUtterances(badUtterances ?? [], translate) : { textItems: [], abuseTypes: [] };
  const abuseVectorLabel = resolveAbuseVectorLabel(abuseVector, translate, overrides?.label);
  const titleDescriptor = resolveTitleDescriptor({
    interventionType,
    durationNs: resolvedDuration,
    abuseVector
  });
  const title = translate(titleDescriptor.key, {
    ...titleDescriptor.params,
    abuseVector: abuseVectorLabel
  });
  const bodyDescriptor = resolveBodyDescriptor({ interventionType, abuseVector });
  const body = translate(bodyDescriptor.key);
  mountTimeMsRef.current ??= monotonicNowMs();
  return {
    status: "ready",
    mountTimeMs: mountTimeMsRef.current,
    view: {
      title,
      body,
      evidence: textItems.join("\n"),
      violationReason: abuseTypes.join(", "),
      isAppealable,
      formattedEndDate,
      countdownText,
      dsaMessage: consequenceTransparencyMessage,
      analytics: {
        interventionId,
        interventionType: interventionTypeForAnalytics,
        acknowledgeable,
        // TODO: Backend should pass duration as seconds
        timeoutDurationSeconds: duration ? duration / NANOSECONDS_PER_SECOND : void 0
      }
    }
  };
};

// src/treatments/dialog/DialogTitleSkeleton.tsx
import { jsx as jsx3 } from "react/jsx-runtime";
var DialogTitleSkeleton = () => {
  return /* @__PURE__ */ jsx3("div", { className: "bg-shift-100 height-[40px] width-[300px] max-width-full radius-medium" });
};
var DialogTitleSkeleton_default = DialogTitleSkeleton;

// src/treatments/dialog/DialogContentSkeleton.tsx
import { jsx as jsx4, jsxs } from "react/jsx-runtime";
var DialogContentSkeleton = () => {
  return /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-medium height-full min-height-0", children: [
    /* @__PURE__ */ jsxs("div", { className: "flex flex-col gap-medium grow-1 scroll-y min-height-0", children: [
      /* @__PURE__ */ jsx4("div", { className: "bg-shift-100 height-[40px] width-full radius-medium" }),
      /* @__PURE__ */ jsx4("div", { className: "bg-shift-100 height-[180px] width-full radius-large" })
    ] }),
    /* @__PURE__ */ jsx4("div", { className: "shrink-0 bg-shift-100 height-[40px] width-full radius-medium" })
  ] });
};
var DialogContentSkeleton_default = DialogContentSkeleton;

// src/treatments/dialog/DialogErrorState.tsx
import { Button, Icon } from "@rbx/foundation-ui";
import { jsx as jsx5, jsxs as jsxs2 } from "react/jsx-runtime";
var DialogErrorState = ({ onDismiss }) => {
  const { translate } = useUniversalFeatureRestrictionsConfig();
  return /* @__PURE__ */ jsxs2("div", { className: "flex flex-col gap-medium height-full min-height-0", children: [
    /* @__PURE__ */ jsxs2("div", { className: "grow-1 flex flex-col items-center justify-center gap-medium padding-xxlarge min-height-[200px]", children: [
      /* @__PURE__ */ jsx5(Icon, { name: "icon-regular-triangle-exclamation", size: "XLarge" }),
      /* @__PURE__ */ jsx5("span", { className: "text-body-large content-emphasis margin-none", children: translate("Heading.Error") })
    ] }),
    /* @__PURE__ */ jsx5("div", { className: "shrink-0", children: /* @__PURE__ */ jsx5(Button, { variant: "Emphasis", size: "Medium", className: "width-full", onClick: onDismiss, children: translate("Action.OK") }) })
  ] });
};
var DialogErrorState_default = DialogErrorState;

// src/treatments/dialog/DialogInterventionDetails.tsx
import { useEffect as useEffect4 } from "react";

// src/treatments/dialog/DialogEvidenceCard.tsx
import { Badge, Divider } from "@rbx/foundation-ui";

// src/shared/components/EvidenceField.tsx
import { jsx as jsx6, jsxs as jsxs3 } from "react/jsx-runtime";
var COLON = ":";
var EvidenceField = ({ fieldLabel, fieldValue, hasColon = true }) => {
  return /* @__PURE__ */ jsxs3("div", { className: "flex flex-col gap-xxsmall", children: [
    /* @__PURE__ */ jsxs3("span", { className: "text-title-medium content-emphasis margin-none", children: [
      fieldLabel,
      hasColon && COLON
    ] }),
    /* @__PURE__ */ jsx6("p", { className: "text-body-medium content-default margin-none", children: fieldValue })
  ] });
};
var EvidenceField_default = EvidenceField;

// src/treatments/dialog/DialogEvidenceCard.tsx
import { jsx as jsx7, jsxs as jsxs4 } from "react/jsx-runtime";
var DialogEvidenceCard = ({
  evidence,
  violationReason,
  formattedEndDate,
  countdownText
}) => {
  const { translate } = useUniversalFeatureRestrictionsConfig();
  if (!evidence && !violationReason && !formattedEndDate) {
    return null;
  }
  const isExpired = !countdownText;
  const badgeText = countdownText ?? translate("Label.Complete");
  return /* @__PURE__ */ jsxs4("div", { className: "flex flex-col gap-large padding-y-large radius-large stroke-default stroke-standard bg-shift-100", children: [
    evidence && /* @__PURE__ */ jsx7(
      "p",
      {
        className: "text-body-medium content-default padding-x-medium margin-none",
        style: { whiteSpace: "pre-wrap" },
        children: evidence
      }
    ),
    evidence && violationReason && /* @__PURE__ */ jsx7(Divider, {}),
    violationReason && /* @__PURE__ */ jsx7("div", { className: "padding-x-medium", children: /* @__PURE__ */ jsx7(EvidenceField_default, { fieldLabel: translate("Label.Reason"), fieldValue: violationReason }) }),
    formattedEndDate && /* @__PURE__ */ jsxs4("div", { className: "padding-x-medium flex flex-row justify-between items-center", children: [
      /* @__PURE__ */ jsx7(
        EvidenceField_default,
        {
          fieldLabel: translate("Label.EndsOn"),
          fieldValue: formattedEndDate,
          hasColon: false
        }
      ),
      /* @__PURE__ */ jsx7(
        Badge,
        {
          variant: "Neutral",
          label: badgeText,
          icon: isExpired ? void 0 : "icon-filled-clock"
        }
      )
    ] })
  ] });
};
var DialogEvidenceCard_default = DialogEvidenceCard;

// src/treatments/dialog/useDialogAppealsLink.ts
import { useEffect as useEffect3 } from "react";

// src/shared/utils/getTimeToInteract.ts
function getTimeToInteract(mountTimeMs) {
  const elapsedMs = Math.max(monotonicNowMs() - mountTimeMs, 0);
  return Math.round(elapsedMs) / MILLISECONDS_PER_SECOND;
}

// src/api/useNotApprovedPageIxp.ts
import { useQuery as useQuery2 } from "@tanstack/react-query";
import { useCallback as useCallback2 } from "react";
var IXP_LAYER_NAME = "UserSafety.NotApprovedPage.UserID";
var useNotApprovedPageIxp = ({ enabled = true } = {}) => {
  const { ixp } = useUniversalFeatureRestrictionsConfig();
  const isEnabled = enabled && ixp !== void 0;
  const { data, isLoading } = useQuery2({
    queryKey: [`ixp/${IXP_LAYER_NAME}`],
    queryFn: async () => {
      if (!ixp) {
        return {};
      }
      try {
        return await ixp.fetchLayer(IXP_LAYER_NAME);
      } catch {
        return {};
      }
    },
    enabled: isEnabled,
    staleTime: Infinity
  });
  const logExposure = useCallback2(() => {
    ixp?.logExposure(IXP_LAYER_NAME);
  }, [ixp]);
  return { data: data ?? {}, isLoading, logExposure };
};

// src/treatments/dialog/useDialogAppealsLink.ts
var useDialogAppealsLink = ({
  appealsDestination,
  analytics,
  mountTimeMs
}) => {
  const { websiteUrl } = useUniversalFeatureRestrictionsConfig();
  const { readOnly } = useRestrictionScope();
  const sendAnalyticsEvent = useSendAnalyticsEvent();
  const hasExplicitDestination = appealsDestination !== void 0;
  const { data: ixpData, logExposure } = useNotApprovedPageIxp({
    enabled: !hasExplicitDestination
  });
  const isEnrolledInSafetyDashboard = "FFlagEnableSafetyDashboard" in ixpData;
  const shouldRedirectToSafetyDashboard = isEnrolledInSafetyDashboard && ixpData.FFlagEnableSafetyDashboard === true;
  useEffect3(() => {
    if (!hasExplicitDestination && isEnrolledInSafetyDashboard && !readOnly) {
      logExposure();
    }
  }, [hasExplicitDestination, isEnrolledInSafetyDashboard, readOnly, logExposure]);
  const target = hasExplicitDestination ? void 0 : "_blank";
  const href = hasExplicitDestination ? appealsDestination.href : shouldRedirectToSafetyDashboard ? getSafetyDashboardAppealsUrl(websiteUrl) : getAppealsPortalUrl(websiteUrl);
  const onClick = (event) => {
    sendAnalyticsEvent("AppealClicked" /* AppealClicked */, {
      timeToInteractSeconds: getTimeToInteract(mountTimeMs),
      ...analytics
    });
    if (appealsDestination?.onNavigate) {
      event.preventDefault();
      appealsDestination.onNavigate();
    }
  };
  return { href, target, onClick };
};

// src/treatments/dialog/DialogInterventionDetails.tsx
import { jsx as jsx8, jsxs as jsxs5 } from "react/jsx-runtime";
var DialogInterventionDetails = ({ view, appealsDestination, mountTimeMs }) => {
  const { translateHtml } = useUniversalFeatureRestrictionsConfig();
  const sendAnalyticsEvent = useSendAnalyticsEvent();
  const {
    body,
    evidence,
    violationReason,
    isAppealable,
    formattedEndDate,
    countdownText,
    analytics
  } = view;
  const { href, target, onClick } = useDialogAppealsLink({
    appealsDestination,
    mountTimeMs,
    analytics: {
      interventionType: analytics.interventionType,
      eventId: analytics.interventionId,
      acknowledgeable: analytics.acknowledgeable
    }
  });
  useEffect4(() => {
    sendAnalyticsEvent("ModalAppeared" /* ModalAppeared */, {
      evidence,
      interventionType: analytics.interventionType,
      eventId: analytics.interventionId,
      timeoutDurationSeconds: analytics.timeoutDurationSeconds,
      acknowledgeable: analytics.acknowledgeable
    });
  }, []);
  return /* @__PURE__ */ jsxs5("div", { className: "flex flex-col gap-medium grow-1 scroll-y min-height-0", children: [
    body && /* @__PURE__ */ jsx8("p", { className: "text-body-medium margin-none content-default", children: body }),
    /* @__PURE__ */ jsx8(
      DialogEvidenceCard_default,
      {
        evidence,
        violationReason,
        formattedEndDate,
        countdownText
      }
    ),
    isAppealable && /* @__PURE__ */ jsx8("p", { className: "text-body-medium content-default margin-none", children: translateHtml("Description.Disclaimer", [
      {
        opening: "startLink",
        closing: "endLink",
        render: (children) => /* @__PURE__ */ jsx8(
          "a",
          {
            href,
            target,
            rel: "noreferrer noopener",
            className: "content-default underline",
            style: { textUnderlineOffset: "3px" },
            onClick,
            children
          }
        )
      }
    ]) })
  ] });
};
var DialogInterventionDetails_default = DialogInterventionDetails;

// src/treatments/dialog/DialogInterventionActions.tsx
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { Button as Button2 } from "@rbx/foundation-ui";
import { jsx as jsx9, jsxs as jsxs6 } from "react/jsx-runtime";
var DialogInterventionActions = ({ onDismiss, analytics, dsaMessage, mountTimeMs }) => {
  const { translate, api } = useUniversalFeatureRestrictionsConfig();
  const { readOnly, abuseVector } = useRestrictionScope();
  const sendAnalyticsEvent = useSendAnalyticsEvent();
  const queryClient = useQueryClient();
  const { interventionId, interventionType, acknowledgeable } = analytics;
  const additionalAnalyticsFields = {
    interventionType,
    eventId: interventionId,
    acknowledgeable
  };
  const { mutate: dismissIntervention, isPending } = useMutation({
    mutationFn: (interventionId2) => api.dismissIntervention(interventionId2),
    onSuccess: () => {
      sendAnalyticsEvent("DialogInterventionDismissSuccess" /* DialogInterventionDismissSuccess */, additionalAnalyticsFields);
      queryClient.removeQueries({ queryKey: moderationDetailQueryKey(abuseVector) });
    },
    onError: (error) => {
      sendAnalyticsEvent("DialogInterventionDismissFailed" /* DialogInterventionDismissFailed */, {
        error: error.message,
        ...additionalAnalyticsFields
      });
    }
  });
  const handleCtaClick = () => {
    sendAnalyticsEvent("CtaClicked" /* CtaClicked */, {
      timeToInteractSeconds: getTimeToInteract(mountTimeMs),
      ...additionalAnalyticsFields
    });
    if (!readOnly && acknowledgeable && interventionId) {
      dismissIntervention(interventionId);
    }
    onDismiss();
  };
  return /* @__PURE__ */ jsxs6("div", { className: "flex flex-col gap-small", children: [
    /* @__PURE__ */ jsx9(
      Button2,
      {
        variant: "Emphasis",
        size: "Medium",
        className: "width-full",
        onClick: handleCtaClick,
        isDisabled: isPending,
        isLoading: isPending,
        children: translate("Action.OK")
      }
    ),
    dsaMessage && /* @__PURE__ */ jsx9("p", { className: "text-caption-small content-default margin-none", children: dsaMessage })
  ] });
};
var DialogInterventionActions_default = DialogInterventionActions;

// src/treatments/dialog/DialogContentView.tsx
import { jsx as jsx10, jsxs as jsxs7 } from "react/jsx-runtime";
var DialogContentView = ({
  onDismiss,
  overrides,
  appealsDestination,
  translationsReady
}) => {
  const model = useDialogRestrictionModel({ overrides, translationsReady });
  if (model.status === "loading") {
    return /* @__PURE__ */ jsxs7(DialogBody, { className: "flex flex-col gap-medium", children: [
      /* @__PURE__ */ jsx10(DialogTitle, { className: "margin-none", children: /* @__PURE__ */ jsx10(DialogTitleSkeleton_default, {}) }),
      /* @__PURE__ */ jsx10(DialogContentSkeleton_default, {})
    ] });
  }
  if (model.status === "error") {
    return /* @__PURE__ */ jsx10(DialogBody, { className: "flex flex-col gap-medium", children: /* @__PURE__ */ jsx10(DialogErrorState_default, { onDismiss }) });
  }
  const { view, mountTimeMs } = model;
  return /* @__PURE__ */ jsxs7(DialogBody, { className: "flex flex-col gap-medium", children: [
    /* @__PURE__ */ jsx10(DialogTitle, { className: "text-heading-medium margin-none", children: view.title }),
    /* @__PURE__ */ jsxs7("div", { className: "flex flex-col gap-medium height-full min-height-0", children: [
      /* @__PURE__ */ jsx10(
        DialogInterventionDetails_default,
        {
          view,
          appealsDestination,
          mountTimeMs
        }
      ),
      /* @__PURE__ */ jsx10("div", { className: "shrink-0", children: /* @__PURE__ */ jsx10(
        DialogInterventionActions_default,
        {
          onDismiss,
          analytics: view.analytics,
          dsaMessage: view.dsaMessage,
          mountTimeMs
        }
      ) })
    ] })
  ] });
};
var DialogContentView_default = DialogContentView;

// src/treatments/dialog/UniversalFeatureRestrictionDialog.tsx
import { jsx as jsx11 } from "react/jsx-runtime";
var createQueryClient = () => new QueryClient({
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false,
      retry: false,
      refetchOnMount: false,
      staleTime: 60 * 1e3
    }
  }
});
var UniversalFeatureRestrictionDialog = ({
  config,
  request,
  open,
  onDismiss
}) => {
  const [queryClient] = useState2(createQueryClient);
  const { abuseVector, readOnly, overrides, appealsDestination } = request;
  return /* @__PURE__ */ jsx11(QueryClientProvider, { client: queryClient, children: /* @__PURE__ */ jsx11(UniversalFeatureRestrictionsConfigProvider, { config, children: /* @__PURE__ */ jsx11(RestrictionScopeProvider, { abuseVector, readOnly, children: /* @__PURE__ */ jsx11(
    Dialog,
    {
      open,
      isModal: true,
      size: "Medium",
      hasCloseAffordance: false,
      onOpenChange: readOnly ? (nextOpen) => {
        if (!nextOpen) onDismiss();
      } : void 0,
      children: /* @__PURE__ */ jsx11(
        DialogContent,
        {
          className: "width-full",
          onOpenAutoFocus: (event) => {
            event.preventDefault();
          },
          children: /* @__PURE__ */ jsx11(
            DialogContentView_default,
            {
              onDismiss,
              overrides,
              appealsDestination,
              translationsReady: config.translationsReady ?? true
            }
          )
        }
      )
    }
  ) }) }) });
};
var UniversalFeatureRestrictionDialog_default = UniversalFeatureRestrictionDialog;
export {
  UniversalFeatureRestrictionDialog_default as UniversalFeatureRestrictionDialog
};
